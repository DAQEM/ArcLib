{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${version}",
  "name": "${mod_name}",
  "description": "${mod_description}",
  "authors": [
    "${mod_author}"
  ],
  "contact": {
    "homepage": "${mod_website}",
    "sources": "${mod_repository}"
  },
  "license": "${mod_license}",
  "icon": "icon.png",
  "environment": "*",
  "entrypoints": {
    "main": [
      "com.daqem.arc.fabric.ArcFabric"
    ]
  },
  "mixins": [
    "arc-common.mixins.json"
  ],
  "accessWidener": "arc.accesswidener",
  "depends": {
    "fabric-api": ">=${fabric_version}",
    "minecraft": ">=${minecraft_version}",
    "knot": ">=${knot_version}",
    "uilib": ">=${uilib_version}",
    "yamlconfig": ">=${yamlconfig_version}"
  }
}